{ storage (option (pair (set %allowed_whitelists nat) (bool %unrestricted))) ;
  parameter (pair (set %allowed_whitelists nat) (bool %unrestricted)) ;
  code { DUP ;
         CDR ;
         SWAP ;
         CAR ;
         DUP ;
         SOME ;
         DUG 2 ;
         DROP 2 ;
         NIL operation ;
         PAIR } }
