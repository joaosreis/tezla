{ parameter (or (or (int %decrement) (int %increment)) (int %sayHello)) ;
  storage int ;
  code { UNPAIR ;
         IF_LEFT { IF_LEFT { SWAP ; SUB } { ADD } } { DROP } ;
         NIL operation ;
         PAIR } }
