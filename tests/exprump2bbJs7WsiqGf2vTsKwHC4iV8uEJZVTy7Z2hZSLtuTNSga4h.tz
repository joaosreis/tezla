{ storage
    (pair (nat %counter)
          (pair (map %instances_map address (list address)) (option %x address))) ;
  parameter unit ;
  code { CDR ;
         SENDER ;
         PUSH nat 0 ;
         PAIR %min_increase %owner ;
         PUSH bool False ;
         PAIR %is_active ;
         SENDER ;
         PUSH mutez 0 ;
         PAIR %current_bid %highest_bidder ;
         DIG 2 ;
         DUP ;
         DUG 3 ;
         CAR ;
         PAIR %asset_id ;
         PAIR ;
         PUSH mutez 0 ;
         NONE key_hash ;
         CREATE_CONTRACT
           { parameter (pair (nat %min_increase) (nat %reserve_price)) ;
             storage
               (pair (pair (nat %asset_id) (pair (mutez %current_bid) (address %highest_bidder)))
                     (pair (bool %is_active) (pair (nat %min_increase) (address %owner)))) ;
             code { { DUP ;
                      CDR ;
                      SWAP ;
                      CAR ;
                      SWAP ;
                      DUP ;
                      DUG 2 ;
                      CDR ;
                      CAR ;
                      IF { { PUSH string "WrongCondition: ~ self.data.is_active" ; FAILWITH } }
                         { {} } ;
                      SWAP ;
                      DUP ;
                      CDR ;
                      SWAP ;
                      CAR ;
                      DUP ;
                      CAR ;
                      SWAP ;
                      CDR ;
                      CDR ;
                      PUSH mutez 1 ;
                      DIG 4 ;
                      DUP ;
                      DUG 5 ;
                      CDR ;
                      MUL ;
                      PAIR ;
                      SWAP ;
                      PAIR ;
                      PAIR ;
                      SWAP ;
                      SWAP ;
                      DUP ;
                      CAR ;
                      SWAP ;
                      CDR ;
                      DUP ;
                      CAR ;
                      SWAP ;
                      CDR ;
                      CDR ;
                      DIG 3 ;
                      CAR ;
                      PAIR ;
                      SWAP ;
                      PAIR ;
                      SWAP ;
                      PAIR ;
                      DUP ;
                      CAR ;
                      SWAP ;
                      CDR ;
                      CDR ;
                      PUSH bool True ;
                      PAIR ;
                      SWAP ;
                      PAIR ;
                      NIL operation ;
                      PAIR } } } ;
         PAIR ;
         DUP ;
         CAR ;
         NIL operation ;
         SWAP ;
         CONS ;
         DIG 2 ;
         DUP ;
         CAR ;
         SWAP ;
         CDR ;
         CAR ;
         DIG 3 ;
         CDR ;
         SOME ;
         SWAP ;
         PAIR ;
         SWAP ;
         PAIR ;
         SWAP ;
         SWAP ;
         DUP ;
         DUG 2 ;
         CDR ;
         PUSH nat 1 ;
         DIG 3 ;
         CAR ;
         ADD ;
         PAIR ;
         SWAP ;
         PAIR } }
